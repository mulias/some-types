export {
  // Namespace and Type
  Empty,
  // Constructors
  value,
  // Typeguards
  isType,
  // Conversions
  fromArray,
  coerce,
};

import { Empty } from "./namespace";
const { value, isType, fromArray, coerce } = Empty;
