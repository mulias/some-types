export {
  // Namespace and Type
  Triple,
  // Constructors
  of,
  // Typeguards
  isType,
  // Conversions
  fromArray,
  coerce,
};

import { Triple } from "./namespace";
const { of, isType, fromArray, coerce } = Triple;
