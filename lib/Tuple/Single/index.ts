export {
  // Namespace and Type
  Single,
  // Constructors
  of,
  // Typeguards
  isType,
  // Conversions
  fromArray,
  coerce,
};

import { Single } from "./namespace";
const { of, isType, fromArray, coerce } = Single;
