export {
  // Namespace and Type
  Pair,
  // Constructors
  of,
  // Typeguards
  isType,
  // Conversions
  fromArray,
  coerce,
};

import { Pair } from "./namespace";
const { of, isType, fromArray, coerce } = Pair;
