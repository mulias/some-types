export {
  // Namespace and Type
  Some,
  // Constructors
  of,
  // Typeguards
  isType,
  // Conversions
  coerce,
  // Operations
  ifType,
};

import { Some } from "./namespace";
const { of, isType, coerce, ifType } = Some;
