import { Ok } from "./namespace";

export { Ok } from "./namespace";
export {
  // Constructors
  of,
  // Typeguards
  isType,
  // Conversions
  coerce,
  // Operations
  ifType,
};

const { of, isType, coerce, ifType } = Ok;
