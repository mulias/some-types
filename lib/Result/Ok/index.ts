export {
  // Namespace and Type
  Ok,
  // Constructors
  of,
  // Typeguards
  isType,
  // Conversions
  coerce,
  // Operations
  ifType,
};

import { Ok } from "./namespace";
const { of, isType, coerce, ifType } = Ok;
