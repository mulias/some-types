export {
  // Namespace and Type
  ValidDate,
  // Constructors
  validDate,
  of,
  now,
  // Typeguards
  isValidDate,
  isType,
  // Conversions
  fromDate,
  toDate,
  asDate,
  coerceValidDate,
  coerce,
  // Operations
  map,
};

import { ValidDate } from "./namespace";
const {
  validDate,
  of,
  now,
  isValidDate,
  isType,
  fromDate,
  toDate,
  asDate,
  coerceValidDate,
  coerce,
  map,
} = ValidDate;
