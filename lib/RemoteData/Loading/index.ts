import { Loading } from "./namespace";

export { Loading } from "./namespace";
export {
  // Constructors
  value,
  // Type guards
  isType,
  // Conversions
  coerce,
  // Operations
  ifType,
};

const { value, isType, coerce, ifType } = Loading;
