export {
  // Namespace and Type
  Loading,
  // Constructors
  value,
  // Type guards
  isType,
  // Conversions
  coerce,
  // Operations
  ifType,
};

import { Loading } from "./namespace";
const { value, isType, coerce, ifType } = Loading;
