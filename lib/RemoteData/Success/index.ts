import { Success } from "./namespace";

export { Success } from "./namespace";
export {
  // Constructors
  of,
  // Type guards
  isType,
  // Conversions
  coerce,
  // Operations
  ifType,
};

const { of, isType, coerce, ifType } = Success;
