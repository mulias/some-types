export {
  // Namespace and Type
  Success,
  // Constructors
  of,
  // Type guards
  isType,
  // Conversions
  coerce,
  // Operations
  ifType,
};

import { Success } from "./namespace";
const { of, isType, coerce, ifType } = Success;
