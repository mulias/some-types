export {
  // Namespace and Type
  ReadonlyDate,
  // Constructors
  readonlyDate,
  of,
  now,
  // Conversions
  fromDate,
  toDate,
  asDate,
};

import { ReadonlyDate } from "./namespace";
const { readonlyDate, of, now, fromDate, toDate, asDate } = ReadonlyDate;
