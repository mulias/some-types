import { DataError } from "./namespace";

// Namespace and Type
export { DataError } from "./namespace";
export {
  // Constructors
  dataError,
  of,
  // Typeguards
  isDataError,
  isType,
  // Conversions
  fromError,
  // Operations
  map,
};

const {
  // Constructors
  dataError,
  of,
  // Typeguards
  isDataError,
  isType,
  // Conversions
  fromError,
  // Operations
  map,
} = DataError;
